export const getNumberOfChars = (yourString) => name.length;

export const getNumberOfCharacters = (yourString) => name.length;

export const getFirstChar = (yourString) => name[0];

export const getLastChar = (yourString) => name[name.length-1];